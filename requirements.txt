# numpy >=2.3 and scipy >=1.16 dropped Python 3.10, so conda_setup_py10.sh needs the
# last releases that still ship cp310 wheels. The py11/py12 setups keep the newer pins.
numpy==2.3.5; python_version >= "3.11"
numpy==2.2.6; python_version < "3.11"
scipy==1.16.3; python_version >= "3.11"
scipy==1.15.3; python_version < "3.11"
pyvista==0.46.4
matplotlib==3.10.7
vtk==9.5.2
pybind11==3.0.1
# viser: web-based interactive visualizer (python/tests/tendon_hand/viz_interactive.py,
# python/tests/_objects/ycb/browser.py). Pulls in trimesh, which ycb/browser.py imports
# directly. 1.1.0 is the latest release that still ships py3.10-compatible deps.
viser==1.1.0
