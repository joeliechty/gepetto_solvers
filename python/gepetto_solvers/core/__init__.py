"""Foundational infrastructure: the hand model, scene geometry, the solver classes,
trajectory tooling, object data and plotting.

Nothing in here may import from ``gepetto_solvers.projects`` or
``gepetto_solvers.experimental`` -- the dependency runs one way, so a project can be
deleted without touching core.
"""
